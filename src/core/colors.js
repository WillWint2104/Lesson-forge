/* ═══════════════════════════════════════════
   CORE — SHARED BRAND COLORS
   ═══════════════════════════════════════════
   Single source of truth for cross-module colors.
   Imported by core/ui.jsx (for the full C palette) and
   core/math.jsx (for NumberLine rendering — kept here to
   avoid a circular import between ui ↔ math). */

export const GREEN = '#5CB89B'
export const GREEN_DK = '#3D9A7E'
export const BLACK = '#1E293B'
